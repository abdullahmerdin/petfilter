{
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/assets/(.*)", "dest": "api/build/client/assets/$1" },
    { "src": "/(.*)", "dest": "api/index.js" }
  ],
  "buildCommand": "npm run setup && npm run build && node -e \"const fs=require('fs');const p='api/build';if(fs.existsSync(p))fs.rmSync(p,{recursive:true});fs.cpSync('build',p,{recursive:true})\"",
  "installCommand": "npm install"
}
